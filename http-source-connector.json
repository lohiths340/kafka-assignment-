{
  "name": "http-source-connector",
  "config": {
    "connector.class": "io.confluent.connect.http.HttpSourceConnector",
    "tasks.max": "1",
    "http.api.url": "http://json-server:3000/orders",
    "kafka.topic": "orders",
    "poll.interval.ms": "1000",
    "batch.max.size": "100"
  }
}
